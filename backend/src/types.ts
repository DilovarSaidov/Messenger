export type Message = {
  text: string;
  time: string;
};

export type Contact = {
  firstName: string;
  lastName: string;
  status: string;
  avatar: string;
};

export type User = {
  firstName: string;
  lastName: string;
  phoneNumber: string;
  password: string;
};
